
name: TestAction

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  log:
    runs-on: windows-latest
    steps:
    - name: Logging
      run: |
        echo "${{ github.event.head_commit.message )}}"
